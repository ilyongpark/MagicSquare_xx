# MagicSquare_1004 — 세션 3 Harness·ECB 골격 + Cursor Rules·Commands

| 항목 | 내용 |
|------|------|
| **프로젝트** | MagicSquare_xx |
| **단계** | STEP 3 — Harness·규칙·커맨드 준비 (구현·RED 테스트 본문 미착수) |
| **보고서 생성일** | 2026-06-10 |
| **목적** | `validate_lines` TDD를 위한 최소 Harness, `.cursorrules`, Cursor Slash Commands 골격 확립 |

---

## 1. 요약

세션 3 구현(TDD RED→GREEN)에 앞서 **코드·규칙·워크플로 골격**을 한 번에 정비했다.

1. **최소 Harness** — `pyproject.toml`(pytest), `src/`, `tests/` 스켈레톤 생성 (본문 비움)
2. **`.cursorrules`** — 도메인(4×4·10선·34), API(`validate_lines` → `{status, failed_lines}`), ECB, TDD·AI 규칙 (50줄)
3. **Harness ECB 갱신** — `entity.py`(MAGIC·LINE_IDS), `ValidateResult` TypedDict, Control/Boundary 분리
4. **규칙 갭 점검** — 10선 좌표·SSOT·incomplete 조건·RED 금지 보강 항목 표로 정리 (파일 미수정)
5. **Cursor Commands** — `tdd-red`, `export-session`, `export` 추가

구현·테스트 함수 본문은 아직 없으며, 다음 단계는 **TDD RED**(대각선만 틀린 케이스)부터 시작한다.

---

## 2. 핵심 결정·산출물

### 핵심 결정

| 결정 | 내용 |
|------|------|
| API 계약 | `validate_lines(grid) -> {"status": "pass"\|"fail"\|"incomplete", "failed_lines": [...]}` |
| ECB 매핑 | Entity=`entity.py`, Control/Boundary=`validate_lines.py` |
| 10선 ID | R1~R4(행), C1~C4(열), D1·D2(대각선) |
| TDD RED | `tests/`만 수정, `src/` 구현 금지, assert 완화·skip·xfail 금지 |
| `.cursorrules` 보강 후보 | 10선 좌표 매핑, MAGIC SSOT 명시, incomplete 발생 조건, RED 세부 금지 |

### 생성·수정 파일

| 파일 | 역할 |
|------|------|
| `pyproject.toml` | pytest 설정, dev 의존성 |
| `src/__init__.py` | 패키지 골격 (빈 파일) |
| `src/entity.py` | `MAGIC`, `LINE_IDS`, `get_line_cells`, `sum_line` 시그니처 |
| `src/validate_lines.py` | `ValidateResult`, `validate_lines` 시그니처 |
| `tests/__init__.py` | 테스트 패키지 (빈 파일) |
| `tests/test_validate_lines.py` | import만 |
| `.cursorrules` | 프로젝트 도메인·ECB·TDD·AI 규칙 |
| `.cursor/commands/tdd-red.md` | `validate_lines` RED 전용 커맨드 |
| `.cursor/commands/export-session.md` | 세션 Export 절차 |
| `.cursor/commands/export.md` | `/export` 즉시 실행 별칭 |

---

## 3. 다음 단계

1. **`.cursorrules` 보강** (선택) — 10선 좌표·SSOT·incomplete·RED 금지 항목 반영
2. **TDD RED** — `/tdd-red` 또는 `tdd-red.md` 절차로 D1만 틀린 격자 테스트 추가
3. **TDD GREEN** — `validate_lines` 최소 구현 (10선 전부 검사)
4. **TDD REFACTOR** — 합산·줄 추출 중복 정리

관련 Transcript: [Prompting/04.Export-Transcript.md](../Prompting/04.Export-Transcript.md)

*본 문서는 Report/04.REPORT.md — 세션 3 Harness·ECB 골격 + Cursor Rules·Commands 준비 보고서입니다.*
